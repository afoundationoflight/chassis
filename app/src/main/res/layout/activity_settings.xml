<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0E0E12"
    android:padding="16dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="the tongue"
        android:textColor="#7A8AA0"
        android:fontFamily="monospace"
        android:textSize="13sp"
        android:paddingBottom="16dp" />

    <!-- LOCAL / REMOTE — the seat never boots without a working
         tongue, so switching to remote without a saved key here just
         leaves local active; see TongueSettings.buildActiveTongue(). -->
    <RadioGroup
        android:id="@+id/tongueChoice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingBottom="20dp">
        <RadioButton
            android:id="@+id/chooseLocal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="local (always available)"
            android:textColor="#D6DBE4"
            android:checked="true" />
        <RadioButton
            android:id="@+id/chooseRemote"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="24dp"
            android:text="remote"
            android:textColor="#D6DBE4" />
    </RadioGroup>

    <EditText
        android:id="@+id/endpointInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="endpoint (e.g. generativelanguage.googleapis.com)"
        android:textColorHint="#5A6272"
        android:textColor="#E8ECF2"
        android:background="#1A1A22"
        android:padding="12dp"
        android:inputType="textUri"
        android:layout_marginBottom="10dp" />

    <EditText
        android:id="@+id/modelInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="model name (e.g. gemini-2.5-flash-lite)"
        android:textColorHint="#5A6272"
        android:textColor="#E8ECF2"
        android:background="#1A1A22"
        android:padding="12dp"
        android:layout_marginBottom="10dp" />

    <!-- MASKED. This is the only field that is never shown in the
         clear, and it is never written anywhere except
         EncryptedSharedPreferences (TongueSettings.kt) — not the
         board table, not a log line. -->
    <EditText
        android:id="@+id/apiKeyInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="API key"
        android:textColorHint="#5A6272"
        android:textColor="#E8ECF2"
        android:background="#1A1A22"
        android:padding="12dp"
        android:inputType="textPassword"
        android:layout_marginBottom="20dp" />

    <Button
        android:id="@+id/saveButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="save" />

    <TextView
        android:id="@+id/statusText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#7A8AA0"
        android:fontFamily="monospace"
        android:textSize="11sp"
        android:paddingTop="12dp" />

</LinearLayout>
